import { PdfViewerDialogDirective } from './pdf-viewer-dialog.directive';

describe('PdfViewerDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new PdfViewerDialogDirective();
    expect(directive).toBeTruthy();
  });
});
