import { FoyerFilterPipe } from './foyer-filter.pipe';

describe('FoyerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FoyerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
