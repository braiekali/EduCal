import { ReserveButtonDirective } from './reserve-button.directive';

describe('ReserveButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ReserveButtonDirective();
    expect(directive).toBeTruthy();
  });
});
